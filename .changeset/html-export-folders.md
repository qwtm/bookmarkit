---
"bookmarkit": patch
---

Keep folder structure, tags, and rating through an HTML export and re-import. HTML export wrote one
flat list, so backing up to HTML and restoring it collapsed every bookmark into a single folder.
